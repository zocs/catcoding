# CatCoding NATS Server 配置
# 监听端口 4222，启用 JetStream

# 基本配置
host: 127.0.0.1
port: 4222

# JetStream 持久化（任务队列、状态存储）
jetstream {
    store_dir: "/tmp/nats/jetstream"
    max_mem: 1G
    max_file: 10G
}

# 日志
debug: false
trace: false
logtime: true

# 客户端连接限制
max_connections: 128
max_payload: 8MB
